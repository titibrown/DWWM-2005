//Exercice 3.1 : Rechercher un nombre dans un tableau
//Soit un tableau de nombres triés par ordre croissant.
//Chercher si un nombre donné N figure parmi les éléments.
//Si oui, indiquer la valeur de l'indice ctableauespondant. Sinon, indiquer « Non trouvé ».






var number = ['2, 4, 6, 8, 10'];
console.log(number.length);

number.length =5;
console.log(number); 

number.length = 0;
console.log(number); 

number.length = 3;
console.log(number);